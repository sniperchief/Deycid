# Only needed when this frontend is deployed separately from the backend
# that serves /api/status and /api/run (src/web/server.ts) — e.g. this site
# on Vercel, the server on Railway/Render/Fly. Leave unset when the same
# server serves both (the default local/self-hosted setup).
VITE_API_BASE_URL=https://your-backend.up.railway.app
